{
  "name": "@personal-agent-os/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "migrate": "pnpm run db:migrate:local",
    "db:validate": "tsx scripts/validate-schema-drift.ts",
    "db:migrate:local": "tsx scripts/flyway.ts migrate",
    "db:migrate:prod": "LOAD_ENV_FILE=false tsx scripts/flyway.ts migrate",
    "db:info": "tsx scripts/flyway.ts info",
    "db:validate:migrations": "tsx scripts/flyway.ts validate",
    "test": "vitest run",
    "test:unit": "vitest run tests/unit",
    "test:integration": "vitest run tests/integration",
    "test:coverage": "vitest run --coverage",
    "lint": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.tests.json --noEmit",
    "format": "echo 'no-op'"
  },
  "dependencies": {
    "@clerk/backend": "^3.4.7",
    "@fastify/cors": "^10.0.0",
    "@personal-agent-os/agent-sdk": "workspace:*",
    "@personal-agent-os/observability": "workspace:*",
    "@personal-agent-os/shared": "workspace:*",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.2",
    "fastify": "^5.0.0",
    "pg": "^8.13.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@testcontainers/postgresql": "11.14.0",
    "@types/pg": "^8.11.10",
    "@vitest/coverage-v8": "^3.2.4",
    "drizzle-kit": "^0.31.10",
    "tsx": "^4.19.0",
    "vitest": "^3.2.4"
  }
}
